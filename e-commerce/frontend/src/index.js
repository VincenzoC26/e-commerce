import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Assicurati di avere l'import corretto per il tuo componente App
import './index.css';

ReactDOM.render(
    <React.StrictMode>
      <App />
    </React.StrictMode>,
  document.getElementById('root')
);

